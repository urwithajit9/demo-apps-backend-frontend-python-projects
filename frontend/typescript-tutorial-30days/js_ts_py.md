Typescript

```ts
type Person = {
  name: string;
  age: number;
  address: string;
};

type PersonKeys = keyof Person;
```

Python

```python
Person ={
      name: "ajit";
  age: 35;
  address: "Seoul";
}

PersonKeys = Person.keys()
```
